import { Piece } from "../types/chessboard.d";

export const makeMoveAbbr = (
  check: boolean,
  checkMate: boolean,
  pawnPromotion: boolean,
  castling: boolean,
  endSquareItem: null | Piece,
) => {};
